Scripts auxiliares do pacote de produção da Torre de Controle.

- `install.ps1`: instalação idempotente
- `start.ps1` / `stop.ps1` / `restart.ps1` / `status.ps1`
- `healthcheck.ps1`
- `database/*`: migrate/check/seed
- `oracle/check-oracle.ps1`
- `service/*.ps1`: auto-start via Task Scheduler
